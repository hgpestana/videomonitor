# babymonitor_uwsgi.ini file
[uwsgi]

# Django-related settings
# the base directory (full path)
chdir           = /var/www/babymonitor
# Django's wsgi file
module          = babymonitor.wsgi

# process-related settings
# master
master          = true
# maximum number of worker processes
processes       = 10
# the socket (use the full path to be safe
socket          = /var/www/babymonitor/babymonitor.sock
chmod-socket    = 666
# clear environment on exit
vacuum          = true
# run as daemon
daemonize=/var/log/uwsgi/babymonitor.log
